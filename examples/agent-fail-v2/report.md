4 of 5 proven. 1 failed.

AC1  ✔  A transient dead letter is re-run once about an hour after it died, again about four hours after the next death, and again after sixteen; one that is a minute short of each boundary is left alone. — With the reaper ticking every 5s: r0-due (61m, requeues 0) -> requeues 1, r1-due (4h01, requeues 1) -> 2, r2-due (16h01, requeues 2) -> 3, each with requeued_at set and status pending; r0-short (59m), r1-short (3h59), r2-short (15h59) unchanged at requeues 0/1/2, requeued_at NULL, status dead_letter. All three boundaries and their off-sides observed in one 30s run. [machine-checked] — evidence: receipt trail (2 steps)
AC2  ✔  With a real sandbox but no GitHub credential, a private repository cannot be cloned, and the incident lands in needs_human for the customer with the repo_access_denied reason. — Real E2B sandbox (<sandbox-id>) created, clone of the private repo failed with 'fatal: could not read Username'; group needs_human, reason_code repo_access_denied, needs_human_at set; job completed, attempts 0, no dead-letter class. (First attempt errored in the harness: 'env -u' was placed after the assignments; corrected and re-driven before the job was ever claimed.) [machine-checked] — evidence: receipt trail (3 steps)
AC3  ✔  When the model runs out of room on a real investigation of a real repository, the job is not retried, the incident is not shown to the customer, and the failure is labelled as ours. — Real investigation of abhishekray07/papermark in an E2B sandbox with FRICTION_INVESTIGATION_MAX_TURNS=1. Worker log: 'SDK threw after a typed result; keeping the typed classification' stop=turns_exhausted, error 'Claude Code returned an error result: Reached maximum number of turns (1)' (the exact prod signature). Job dead_letter, attempts 1, requeues 0, last_error 'Friction investigation incomplete: no_verdict_submitted'; group analyzing, reason_code NULL. No 'API call failed' or 'retry the job' line. Class observed: agent, not limit (see Not checked finding). [machine-checked] — evidence: receipt trail (3 steps)
AC4  ✔  If the model provider answers 529 to every call, the job is retried with backoff and finally dead-lettered as transient; the incident is never written to the customer as an evidence verdict. — Provider stub answered 529 to 37 requests. Worker log: typed api_error kept, then 'Friction investigation model unavailable (HTTP 529)' thrown; job pending with attempts 1 and no dead-letter class, group analyzing, reason_code NULL, zero diagnosis_decisions rows. The '>= 2 attempts within the window' part of the expectation was not reached: the Claude Code CLI retries a 529 internally for about three minutes before surfacing it, so the 480s window held one full attempt plus a second in progress (the worker was killed mid-attempt, CLI exit 143). The retry path through the queue, and the absence of any customer verdict, were observed. [machine-checked] — evidence: receipt trail (3 steps)
AC5  ✘  The daily route map for a real app finishes within its (route-scaled) budget instead of dying, and every discovered route gets a row. — Job completed; route_map has 2 rows, not 40-120. The expectation was built on a wrong premise (mine, marked invented in spirit): the route classifier does not discover routes from the repository, it classifies the routes observed in the project's incidents (log: 'Product context repository prepared route_count=1', the single seeded page_url). With route_count=1 the log shows max_turns=21 and budget_usd=0.53, which is exactly the route-scaled formula, and the run cost $0.05. So the scaling rule is visible in the run, but the criterion as approved fails, and its comparison against 80 page files is meaningless. A second route_map job the ingestion sweeper enqueued for the private project failed 'could not reach the model' at 23:01:09 because the bounded host worker was being killed at that moment (harness artifact, retriable). [agent-reported] — evidence: receipt trail (3 steps)

Proven     4 of 5
Behaviour  4 passed, 1 failed
Ran        5 criteria, 0 could not run
Covered    5 criteria, 1 changed files have none

Not checked
  Finding: turn exhaustion in a friction investigation is classed agent, not limit      investigate-friction.ts maps stop turns_exhausted to 'no_verdict_submitted', which the handler classes agent. Behavior is identical (one execution, re-run on the next boot) but the plan's failure table says limit, and the Slack event and /health count it under agent.
  Finding: a 529 outage costs about three minutes per attempt before the queue sees it  the Claude Code CLI retries overloaded errors internally; three queue attempts with backoff take roughly ten minutes. Not wrong, but worth knowing when reading dead-letter timestamps.
  AC5 premise                                                                           route_map classifies observed incident routes, not repository routes; the plan's Task 6 speaks of '59 distinct routes with incidents' for AMFJ 2, which is consistent. Verifying the scaling rule at a realistic route count needs seeded incidents on many distinct page URLs (or the AMFJ 2 data); not done here.
  Harness deviations, disclosed                                                         the stack worker was stopped right after boot so only the per-criterion host workers claimed jobs; AC2's first drive attempt failed on an 'env -u' argument-order typo and was corrected and re-driven; every host-worker boot also requeued deploy-fixable dead letters from earlier criteria (AC3's job was re-run by AC4's worker against the 529 stub), which did not affect any read.
  Reviewer asks not driven                                                              controlled clock exactly at the boundaries, a provider that fails once then succeeds, and multiple route counts for the scaling rule.
  Uncovered changed file                                                                docs/superpowers/plans/2026-09-02-investigation-failure-classification.md (the plan itself).
  Sink and 529 stub                                                                     verified by hand with one request each before boot; no pipeline probe configured. Sink received host-worker events (job_dead_lettered, job_requeued) during the run; not asserted by any criterion in this run.

