# Browse Agent Eval Baselines

This directory stores frozen browse-agent eval measurements.

`v1-baseline.json` is the initial deterministic replay baseline captured before any
browse-agent hardening work. Future changes should compare against it instead of
judging improvements anecdotally.
