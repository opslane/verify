You are filling in values for a database INSERT sequence.

ALREADY EXIST (do NOT create these — use the given IDs):
- User: id=9 (already exists)
- Team: id=7 (already exists)
- Organisation, OrganisationClaim, OrganisationGlobalSettings, OrganisationAuthenticationPortal, TeamGlobalSettings: already exist for this user's team
Only create rows for tables NOT listed above.

ENTITY GRAPH (tables to populate, in order — SKIP tables that already exist):
1. TeamGlobalSettings: [id (text)]
2. OrganisationClaim: [id (text), createdAt (timestamp), updatedAt (timestamp), teamCount (int), memberCount (int), flags (jsonb), envelopeItemCount (int)]
3. User: [email (text), identityProvider (enum: DOCUMENSO, GOOGLE, OIDC), createdAt (timestamp), lastSignedIn (timestamp), updatedAt (timestamp), twoFactorEnabled (bool), disabled (bool)]
4. OrganisationGlobalSettings: [id (text), documentVisibility (enum: EVERYONE, MANAGER_AND_ABOVE, ADMIN), documentLanguage (text), includeSenderDetails (bool), includeSigningCertificate (bool), typedSignatureEnabled (bool), uploadSignatureEnabled (bool), drawSignatureEnabled (bool), brandingEnabled (bool), brandingLogo (text), brandingUrl (text), brandingCompanyDetails (text), emailDocumentSettings (jsonb), documentDateFormat (text), includeAuditLog (bool), aiFeaturesEnabled (bool), delegateDocumentOwnership (bool)]
5. OrganisationAuthenticationPortal: [id (text), enabled (bool), clientId (text), clientSecret (text), wellKnownUrl (text), defaultOrganisationRole (enum: ADMIN, MANAGER, MEMBER), autoProvisionUsers (bool)]
6. TeamMemberInvite: []
7. Organisation: [id (text), createdAt (timestamp), updatedAt (timestamp), type (enum: PERSONAL, ORGANISATION), name (text), url (text), ownerUserId (int) ref:User.id, organisationClaimId (text) ref:OrganisationClaim.id, organisationGlobalSettingsId (text) ref:OrganisationGlobalSettings.id, organisationAuthenticationPortalId (text) ref:OrganisationAuthenticationPortal.id]
8. Team: [name (text), url (text), createdAt (timestamp), organisationId (text) ref:Organisation.id, teamGlobalSettingsId (text) ref:TeamGlobalSettings.id]

CONDITION: An organisation has pending admin and member invitations
userId=9, teamId=7

Output JSON with this exact shape:
{"inserts":[{"table":"TableName","values":{"col":"val",...}},...]}}

LEARNINGS (from past failures — follow these):
- DocumentData.type MUST be 'BYTES_64' (not S3_PATH or BYTES)
- DocumentData.data should be empty string '' for test data
- Envelope.source MUST be 'DOCUMENT' (even for templates)
- Envelope.internalVersion MUST be 1
- Envelope.qrToken is NOT NULL — use gen_random_uuid()

Rules:
- Use gen_random_uuid() for uuid/text ID columns (write it as a string "gen_random_uuid()")
- Use enum values from the LEARNINGS above when specified, otherwise first enum value
- For FK ref columns, use the SAME id string as the referenced table's id
- Set userId=9, teamId=7 — do NOT create new User/Team/Org rows
- SKIP tables listed in ALREADY EXIST section
- For text columns, use realistic placeholder values
- Output ONLY the JSON, no explanation