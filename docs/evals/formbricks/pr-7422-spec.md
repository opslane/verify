## Context
Formbricks PR #7422 — Add Feedback Records page to the Unify workspace.
New client component that renders feedback records in a paginated table with refresh capability.

## Acceptance Criteria
- The Unify workspace sidebar has a "Feedback Records" navigation link
- Navigating to the Feedback Records page shows a table with feedback record rows
- The table has a pagination control for moving between pages
- A refresh button is present on the page to reload the records
